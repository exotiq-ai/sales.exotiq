# Netlify build/runtime artifacts — never user code.
.netlify/

# Local Netlify CLI / env files (keep secrets out of git).
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
*.log
node_modules/
